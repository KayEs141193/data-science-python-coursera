######### Distributions ############

# Binomial Distribution: np.random.binomial(n,p,sims) # each sims provided number of success when n experiments are conducted
# Uniform Distribution: np.random.uniform(0,1)
# Normal Distribution: np.random.normal(mu,sigma,size=k)
# Chi-squared Distribution: np.random.chisquare(deg_of_freedeom,size)
# Bimodal Distribution: 

# import scipy.stats as stats
# stats.kurtosis(distribution_samples) # -ve flatter than normal, +ve peakier than normal
# stats.skew(Dist_samples)

######### Hypothesis Testing ###########

# T-test: stats.ttest_ind(a,b,)